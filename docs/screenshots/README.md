# UI previews

These screenshots were captured from the implemented application components while exercising actual reference-Core/API behavior in the generator's restricted component test harness. Sources are fictional demo documents.

The renderer was the environment's preinstalled React 18.2, TSX was syntax-transpiled, requests were bridged to FastAPI TestClient, and metadata used the test-only transactional store. They are **not** screenshots of a verified React 19/Vite/nginx/PostgreSQL/Compose deployment. See `../BUILD_STATUS.md`.

Capture new real-deployment screenshots under `local/` (ignored by default so private document content is not accidentally committed). Review them before deliberately publishing any.
